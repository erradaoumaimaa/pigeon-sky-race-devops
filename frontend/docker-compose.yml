version: '3.8'
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    volumes:
      # Optional: For development, mount source code
      - ./frontend:/app
    environment:
      - NODE_ENV=production
